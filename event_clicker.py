import pyautogui

# Specify the pixel coordinates where you want to click
x_coordinate = 100
y_coordinate = 200

# Move the mouse to the specified coordinates
pyautogui.moveTo(x_coordinate, y_coordinate)

# Perform a mouse click (left-click)
pyautogui.leftClick()
